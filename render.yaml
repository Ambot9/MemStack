services:
  - type: web
    name: memstack
    runtime: docker
    plan: free
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: DATABASE_PATH
        value: /var/data/memstack.db
      - key: GitPersistence__Enabled
        value: "false"
